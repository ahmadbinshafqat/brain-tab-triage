# Tab Triage is a local-only React app and does not require secrets.
# Optional: change the local storage namespace if embedding multiple builds on one browser profile.
VITE_STORAGE_KEY=tab-triage-items
