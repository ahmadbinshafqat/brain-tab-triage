# brain-tab-triage
A tiny browser-based workspace that lets you quickly sort messy links into three buckets: keep, later, or toss. It uses a lightweight relevance scorer so you can clean up a reading pile in seconds instead of staring at an endless list.
